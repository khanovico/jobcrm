# UX And Performance Audit - 2026-04-24

## Scope
- Static audit of frontend UX, API route shape, backend repository behavior, and agent-facing flows.
- Severity scale: Critical, High, Medium, Low.
- No runtime benchmark was run; findings are code-evidence based.

## Issues

| Done | ID | Severity | Area | Short Description | Evidence | Why It Matters | Suggested Direction |
| --- | --- | --- | --- | --- | --- | --- | --- |
| [x] | UXPERF-001 | Critical | Backend persistence | Mongo writes delete and reinsert every collection on most mutations. | Fixed by replacing normal Mongo write-path `_sync()` calls with targeted per-document `replace_one` / `delete_one` persistence, request-local batch coalescing, and `bulk_write(ordered=False)` flushes when batching. Regression tests assert no `delete_many({})` full rewrites for company/application cascades, notification bulk writes, and clear-company reset/archive paths. | Every small user action or agent write becomes O(total database size). Concurrent writes can lose data because one request rewrites stale in-memory collections over another request's changes. | Done for mutation/write amplification. Broader process-local read/cache architecture remains tracked under UXPERF-003/004. |
| [x] | UXPERF-002 | Critical | Agent bulk writes | Bulk company updates can trigger dozens of full database rewrites in one request. | Fixed by wrapping `/api/v1/agent/companies/bulk` in `repo.bulk_persistence()`, using `ContextVar` request-local batch state, coalescing changed docs per collection, and flushing with Mongo `bulk_write`. Regression coverage verifies one route batch, request-local isolation, flush-on-exception semantics, and actual `bulk_write(ordered=False)` usage. | A 50-company bulk enrichment can become about 100 full database rewrites, blocking the agent workflow and making production latency explode. | Done for repeated full rewrites and per-row flushes. Remaining in-memory scans are bounded by the current max 50 updates and tracked by UXPERF-003/004 plus future indexed read/query work. |
| [ ] | UXPERF-003 | High | Backend architecture | Production reads load entire Mongo collections into process memory. | `MongoRepository._load()` reads every collection with `.find()` at startup in `crm/app/repository.py:1386`; list methods then operate on dictionaries in memory. | Startup time and memory grow with database size. Multiple API processes can drift stale because reads do not query Mongo after startup. | Query Mongo directly for read paths. Keep memory repository for tests/dev only. |
| [x] | UXPERF-004 | High | API list performance | List endpoints scan, filter, and sort full in-memory collections before slicing. | Fixed for Mongo list hot paths: companies, profiles/profile ids, applications, audit events, and notifications now use Mongo `find` filters plus `sort`, `skip`, and `limit`, with supporting indexes and page-bounded company/PPA/profile enrichment. Regression coverage checks query shape, pagination, legacy-normalized records, audit date bounds, notification unread filtering, profile-id frozen filtering, and index creation. | Pagination does not bound server work. Every list page still costs O(all rows), and sort/search cannot use indexes. | Done for Mongo-backed list pagination/filter/sort work. Startup full collection loading remains tracked by UXPERF-003; contains-search and richer list payload reductions remain covered by UXPERF-007/018 and future query tuning. |
| [x] | UXPERF-005 | High | API safety | Several human list endpoints accept unbounded `limit`. | Fixed by adding `Query(..., ge=..., le=200)` bounds to human list routes and regression coverage for oversized `limit=201`. | A user, bug, or browser call can request huge pages, causing large payloads and full render work. | Done for bounded limits. Pagination metadata remains covered by UXPERF-022. |
| [x] | UXPERF-006 | High | Dashboard API | Dashboard metrics fetch up to 10,000 application rows and 200 notifications, then count in Python. | Fixed by `dashboard_application_counts()` and exact unread counts; Mongo repository uses `count_documents()` for application aggregates. | Dashboard first load grows with application count and can undercount unread notifications above 200. | Done for dashboard count payload and Mongo aggregate path. Broader in-memory production reads remain covered by UXPERF-003/004. |
| [ ] | UXPERF-007 | High | Search API | Global search scans long text fields in memory and returns weak application labels. | Search scans company overview, profile bio, and application notes in `crm/app/repository.py:1074`; dashboard displays application result as status plus id slice in `app/src/pages/DashboardPage.tsx:100`. | Search gets slower as rich AI content grows, and application results are hard for users to recognize. | Add indexed/full-text search, searchable application company/job fields, and result summary fields like company name, job title/link, status, updated date. |
| [x] | UXPERF-008 | High | Agent queues | Agent queue endpoints scan all matching applications/companies and return full objects. | Fixed by compact `AgentApplicationTaskSummary` / `AgentCompanyResearchTaskSummary` queue responses, Mongo queue reads using indexed status/research-status queries with `created_at` FIFO sort, bounded `limit`, and projections. Full follow-up reads are available through `GET /api/v1/agent/applications/{id}` and new `GET /api/v1/agent/companies/{id}`. Tests cover compact route shapes, Mongo query/projection shape, legacy status mapping, and the company detail follow-up route. | Polling agents amplify O(all rows) scans. Returning full application/company bodies wastes payload when workers only need id, status, and priority fields first. | Done for queue scan/payload amplification. Legacy compatibility branches remain a non-blocking migration/tuning follow-up under broader read architecture work. |
| [x] | UXPERF-009 | High | Application detail API | Application detail bundles PPA emails with per-PPA scans over all emails. | Fixed by adding batched `list_emails_for_ppas()` and `list_profile_names_by_ids()` repository calls, using one indexed Mongo `$in` query for all PPA emails sorted by `(per_profile_application_id, created_at)` plus one projected profile-name lookup. Detail route no longer calls `list_emails_for_ppa()` or `get_profile()` inside the PPA render loop; tests assert the batched calls and Mongo query/projection shape. | Detail load cost becomes O(PPAs * all emails). Rich outreach history will make the primary review screen slow. | Done for the N+1 query root cause. Summary-first/lazy email body loading remains a future payload-size enhancement, not required for this row. |
| [x] | UXPERF-010 | Medium | Applications page | Applied-profile filter loads all profile summaries on mount and again after every list load. | Fixed by removing the unbounded `listAllProfileSummaries()` helper from `app/src/api.ts` and replacing profile filter options with `/api/v1/applications/applied-profile-facets`. Frontend tests assert no `/profiles/summary` calls from Applications, backend tests cover the compact facets endpoint, and build/backend suites pass. | Application list fetch count grew with profile count and repeated during paging/sorting. | Done. Worker-state refetch decoupling remains tracked separately under UXPERF-013. |
| [x] | UXPERF-011 | Medium | Applications UX | Company search and applied-profile filters only filter the current page. | Fixed by sending `company_search` and repeated `applied_profile_names` params to `/api/v1/applications`; backend InMemory/Mongo repositories filter server-side, frontend resets to page 1 on filter changes, and tests cover company search, profile filter params, and current-mode refetch after mark-applied. | Users could see "No applications in this view" even when matches existed on another page. Filters felt unreliable. | Done for server-backed filters. Pagination metadata remains tracked under UXPERF-022/027. |
| [x] | UXPERF-012 | Medium | Applications UX | Applied-profile filter UI presents all profile names, not only profiles present in current result set. | Fixed by deriving filter choices from server facets for the current base filters, renaming the reset action to "Reset profile filter", raising the bounded facet cap to 500, and adding a 200-profile regression. Mongo uses aggregation for applied-profile facets/filtering with tests covering pipeline shape. | Filter choices could be irrelevant to the current application result set and hide rows unexpectedly. | Done. If profile facets regularly approach 500, add search/virtualization as a future scale enhancement. |
| [x] | UXPERF-013 | Medium | Worker state UX/perf | Non-admin list pages repeatedly call admin-only worker settings and swallow failures. | Fixed by adding authenticated `/api/v1/workers/summary`, keeping `/api/v1/settings/workers` admin-only, and moving Companies/Applications worker banners to a brief shared cache. Applications also refreshes the worker summary on foreground without tying it to every list/page/filter load; tests assert list mode changes do not hit the admin worker route. | Non-admin users generate avoidable 403s on every list load. Admin users also pay another request for each page/sort/filter change. | Done. Worker summary forced refreshes are in-flight deduped; reviewer noted global worker-state denormalization is unnecessary at current scale. |
| [x] | UXPERF-014 | Medium | Company detail | Company detail fetches full industry catalog, and the picker renders unbounded `<option>` and suggestion lists. | Fixed by replacing the fetch-all industry cache with `/api/v1/industries/options`, selected-id hydration, 20-option bounded search, and a custom picker that keeps selected labels visible without a full catalog. Review hardening also gates the first picker request until the company is loaded. | A large taxonomy makes company detail slow and creates a large DOM. | Done. Selected-id hydration is capped at 100 IDs and Mongo option/list paths use query-level paging. |
| [x] | UXPERF-015 | Medium | Industries UX | Industries page only fetches 200 rows and has no pagination. | Fixed by adding `/api/v1/industries/count`, page-sized `limit=25` industry list requests, server-backed search, range/total text, and pagination controls. Count refetches are decoupled from simple page navigation and refresh after create/update/delete. | Users cannot browse beyond the first 200 industries unless search happens to find them. The "Visible industries" stat is not total count. | Done. Broader shared pagination metadata remains tracked under UXPERF-022/027. |
| [x] | UXPERF-016 | Medium | Notifications API/UI | Notification polling uses two requests every 45 seconds and list enrichment does per-row entity lookups. | Fixed by `/api/v1/notifications/summary`, which returns unread count plus bounded newest unread rows in one request; layout poll and inbox-change refresh now use that endpoint. | Multiple tabs/users create constant background load. Per-row enrichment gets more expensive as notification volume grows. | Done for request-count reduction and bounded poll payload. Link-enrichment storage/indexing remains covered by broader notification/list architecture work. |
| [x] | UXPERF-017 | Medium | Notifications UX/perf | Bulk mark-read sends one request per selected notification. | Fixed by `POST /api/v1/notifications/read`, frontend bulk API usage, duplicate-id dedupe, and max 200 ids per request. | Even page-sized batches create request bursts and partial-failure ambiguity. | Done for bulk mark-read request fan-out. |
| [x] | UXPERF-018 | Medium | Companies API payload | Company list endpoint returns full `Company` objects for table views and search pickers. | Fixed by adding compact `CompanyListItem` and `/api/v1/companies/summary`, using Mongo projection for summary reads, and moving Companies page, cached company summaries, and NewApplicationModal search to the summary endpoint. Backend tests cover compact fields, filters/sort/pagination, and Mongo projection; frontend tests assert summary endpoint usage. | Tables and pickers do not need overview, detailed research links, source links, and full enrichment fields. Payload grows as AI research content grows. | Done for table/search payload. Scalability review found the existing `has_application` distinct approach acceptable at tested baseline but flagged application-count denormalization as a future larger data-model improvement. |
| [x] | UXPERF-019 | Medium | Company state freshness | Company list page cache can show stale rows for up to 5 minutes after agent or other-tab changes. | Fixed by reducing company summary TTL to 30 seconds, force-refreshing Companies on foreground return with focus/visibilitychange coalescing, and invalidating company summary cache after create/archive/application-create paths that change list state. Regression tests cover cached revisit, foreground refresh coalescing, and Applications-created cache invalidation. | Agent research/status updates may not appear when users navigate back to the list, causing trust issues. | Done. The list still preserves a cached shell for quick revisits while refreshing on explicit or foreground actions. |
| [ ] | UXPERF-020 | Medium | Profile/industry destructive UX | Several destructive actions use native confirm dialogs with little context and no impact preview. | Profile freeze/delete in `app/src/pages/ProfilesPage.tsx:115`; profile detail delete in `app/src/pages/ProfileDetailPage.tsx:161`; industry delete in `app/src/pages/IndustriesPage.tsx:145`; email delete in `app/src/pages/ApplicationDetailPage.tsx:411`. | Native confirms are easy to misclick, inconsistent with app modals, and cannot show related impact or recovery guidance. | Replace with app modals for deletes/freezes. Show affected related rows, require typed confirmation for high-impact deletes, and disable while submitting. |
| [ ] | UXPERF-021 | Low | Profile UX | Profile save navigates away immediately, with no success state or unsaved-change protection. | `ProfileDetailPage` navigates to `/profiles` after save in `app/src/pages/ProfileDetailPage.tsx:145`. | Long markdown edits can feel risky; accidental navigation or failed mental confirmation hurts confidence. | Show inline saved state and stay on detail, or add unsaved-change guard for dirty long-form forms. |
| [ ] | UXPERF-022 | Low | Pagination UX | Tables infer `hasNextPage` from page length and show no total count. | Applications sets `hasNextPage` from `items.length === PAGE_SIZE` in `app/src/pages/ApplicationsPage.tsx:156`; same pattern appears in Companies, Profiles, Notifications, Audit. | Exact-final-page sizes can show a dead next page, and users cannot tell result size or jump to page. | Return `{items,total,has_next}` or cursor metadata from API and update `TablePagination` to show result count and robust next state. |

## Deep Frontend UX Findings

| Done | ID | Severity | Feature | Short Description | Evidence | Why It Matters | Suggested Direction |
| --- | --- | --- | --- | --- | --- | --- | --- |
| [x] | UXPERF-023 | High | Auth/login | Login screen ships with demo credentials prefilled and has no submit busy state. | Fixed by empty non-dev defaults, email/autocomplete attributes, disabled busy state, and login tests. | In production, prefilled credentials look unsafe and can cause mistaken logins. Slow login allows double submit with no feedback. | Done. |
| [x] | UXPERF-024 | Medium | Auth/routing | Admin-only routes silently redirect non-admin users to dashboard. | Fixed by explicit not-authorized route and route-guard tests. | Users who open Settings/Audit via URL or stale nav get dumped on dashboard without understanding permission failure. | Done. |
| [ ] | UXPERF-025 | High | Shared modals | Any backdrop, close button, or Esc close can discard edited form state without warning. | Shared `Modal` calls `onClose()` for `close` and backdrop/escape in `app/src/components/Modal.tsx:40`; create/edit company closes and clears state in `app/src/pages/CompaniesPage.tsx:307`; application modal resets state in `app/src/components/NewApplicationModal.tsx:64`. | Long edits in company/application/profile-related modals can disappear accidentally. | Add dirty-state guards for forms, make destructive-close explicit, and restore focus to the opener. |
| [ ] | UXPERF-026 | Medium | Shared tables | Clickable table rows are mouse-only patterns without row links or keyboard affordance. | Rows use `onClick={() => navigate(...)}` in Companies and Applications (`app/src/pages/CompaniesPage.tsx:245`, `app/src/pages/ApplicationsPage.tsx:341`) with no `role`, `tabIndex`, or primary link in the first cell. | Keyboard and assistive-tech users cannot easily activate rows; click targets compete with action buttons. | Put actual links in primary cells, keep action buttons separate, and remove row-level click unless keyboard behavior is implemented. |
| [ ] | UXPERF-027 | Medium | Pagination | Pagination only shows past/current pages plus maybe next, with no total, page size, or result range. | `TablePagination` computes pages ending at current page and only renders `page + 1` if `hasNextPage` in `app/src/components/TablePagination.tsx:8`. | Users cannot understand how much data exists, jump forward, or confirm they reached the true end. | Add total count or cursor metadata, result range text, page size, and robust first/last controls when count is available. |
| [x] | UXPERF-028 | High | New application flow | Creating an application requires an existing company; the modal cannot create a company from typed company name. | Fixed by replacing the create-mode empty state with company name, optional website, optional job post, and `bootstrapApplication()` submit support. Duplicate active companies and archived-company restore conflicts now show inline actions. Regression coverage verifies typed creation, duplicate reuse, archived restore, Companies page Apply path, parent rerender field preservation, and build success. | PRD says application creation starts with company name; the old flow forced context switch and extra page navigation. | Done. Remaining compact company search payload/index tuning stays tracked under UXPERF-018/search follow-ups. |
| [x] | UXPERF-029 | High | New application flow | Company search plus select auto-selects the first option when options change. | Fixed by removing the first-result auto-select effect. Search results are bounded to `limit=20`, existing-company use requires an explicit click, selected company is shown as a chip, and stale search results after clear/close are ignored. Regression coverage verifies no auto-select, explicit existing selection, typed-name restore, stale clear, and stale close/reopen. | A user typing a search could accidentally create/edit an application for the first matching company, not an intentionally selected company. | Done. Request aborting is a non-blocking future optimization; stale UI corruption is covered. |
| [x] | UXPERF-030 | Medium | Applications list | Company filter looks like server search but filters only currently loaded rows. | Fixed by removing current-page `companySearchFilteredItems`; the input now debounces into `company_search` for `/api/v1/applications`, and tests verify server query plus page reset behavior. | Users saw empty/partial results even when matches existed on another page. | Done for server-backed company search. Search indexing/denormalization remains a future scale optimization beyond current targets. |
| [x] | UXPERF-031 | Medium | Applications list | Marking applied changes the list mode and moves the row away immediately. | Fixed by keeping the active list mode stable and refetching the current filtered page after `markApplied`; tests assert Pending remains active and the server-refetched row disappears only because the current filter excludes it. | Users lost visual context and could think the table jumped. | Done. Toast/undo remains a future polish option, not required for this row. |
| [ ] | UXPERF-032 | High | Applications list/detail | Several inline async actions have no local busy state or error handling. | Applications list `markApplied` awaits without try/catch in `app/src/pages/ApplicationsPage.tsx:393`; Application detail mark applied/email sent in `app/src/pages/ApplicationDetailPage.tsx:308`; email mark/delete in `app/src/pages/ApplicationDetailPage.tsx:390`. | Failed updates can become silent unhandled rejections, and repeated clicks can send duplicate requests. | Wrap all row/detail actions in reusable action state: busy id, disabled buttons, inline error/toast, optimistic rollback. |
| [ ] | UXPERF-033 | High | Application detail | Primary review workflow is a long undifferentiated stack of PPA analysis and emails. | Detail renders every PPA, analysis, emails, subject buttons, recipient editor, and full sanitized email body inline in `app/src/pages/ApplicationDetailPage.tsx:345`. | The main job-to-apply decision is buried; users cannot scan top recommendations, missing artifacts, or action readiness. | Add a summary header: recommended profile(s), readiness checklist, resume link, chosen recipient/subject, next action. Collapse long analysis/email bodies by default. |
| [ ] | UXPERF-034 | Medium | Application detail | "Clear" button is vague for destructive reset. | Application detail shows a small `Clear` button near status in `app/src/pages/ApplicationDetailPage.tsx:276`; modal title clarifies only after click in `app/src/components/ClearApplicationToPendingModal.tsx:44`. | "Clear" does not communicate it deletes PPA rows and emails; high-risk action hides behind a short label. | Rename to "Reset preparation" or "Clear prep artifacts" and require explicit confirmation for deleting generated assets. |
| [ ] | UXPERF-035 | Medium | Application detail | Application-level "email sent" can diverge from individual email sent states. | Header toggles application `email_sent` in `app/src/pages/ApplicationDetailPage.tsx:315`; each email also toggles sent in `app/src/pages/ApplicationDetailPage.tsx:387`. | Users can mark the application sent without marking a specific email, or vice versa, creating contradictory tracking. | Drive application email state from email records or require selecting which email was sent. Show consistency warnings. |
| [ ] | UXPERF-036 | Medium | Application detail | Subject selection appears under each email but updates the PPA-level cold email plan. | Subject buttons are rendered inside each email card in `app/src/pages/ApplicationDetailPage.tsx:430`; `updateActiveSubject` updates `ppa.cold_email_plan` in `app/src/pages/ApplicationDetailPage.tsx:144`. | Users may think they are choosing a subject for one email, while changing shared plan state across the PPA. | Move subject chooser to PPA-level header or make email-specific subject state explicit. |
| [ ] | UXPERF-037 | Medium | Application detail | Recipient editing is PPA-scoped but launched from an email card. | `editingRecipientPpaId` stores only PPA id in `app/src/pages/ApplicationDetailPage.tsx:100`; "Edit recipient" is shown per email in `app/src/pages/ApplicationDetailPage.tsx:556`. | Multiple emails under one PPA can appear to have separate recipients, but editing one changes shared plan recipient fallback. | Show recipient editor at the scope it modifies, or edit the email's `to` field directly. |
| [x] | UXPERF-038 | Medium | Company list | "Applied" column/filter actually means "has applications". | Fixed by renaming column/filter copy to "Applications" and using "No application records" / "Has application records" values. | Users can read it as submitted applications, not simply associated application records. | Done for wording. Counts require a summary payload change and are not part of this wording fix. |
| [ ] | UXPERF-039 | Medium | Company create | Archived-company restore is a hidden two-submit flow. | Create conflict sets `awaitingArchivedRestore` after failed submit in `app/src/pages/CompaniesPage.tsx:344`; the same submit button changes to "Confirm restore" in `app/src/pages/CompaniesPage.tsx:355`. | Users do not see the archived record details or consequences before the second submit. | Use a dedicated restore modal with company details, archive reason, and explicit "Restore archived company". |
| [ ] | UXPERF-040 | High | Company detail | Company page mixes read-only research review and full edit form on one long page. | Edit form starts after applications in `app/src/pages/CompanyDetailPage.tsx:463` and includes all research fields through `app/src/pages/CompanyDetailPage.tsx:593`. | Users can accidentally edit AI-generated source-of-truth fields while trying to review. Long page creates scroll fatigue. | Split into tabs/sections: Overview, Applications, Research Artifacts, Edit. Lock AI fields behind explicit edit mode. |
| [ ] | UXPERF-041 | Medium | Company detail | Non-indexed enrichment is hidden from review but still editable lower down. | Hidden-message branch says enrichment remains in edit form in `app/src/pages/CompanyDetailPage.tsx:267`; edit fields render later in `app/src/pages/CompanyDetailPage.tsx:558`. | Important stored info can be invisible unless user scrolls into edit mode, making status transitions confusing. | Show collapsed "Stored but hidden because status is X" preview with direct review/edit action. |
| [ ] | UXPERF-042 | High | Clear company research | Clear-company modal offers ambiguous destructive choices. | Buttons are "Archive" and "Clear" in `app/src/components/ClearCompanyResearchModal.tsx:121`; text says Clear removes per-profile rows/emails in `app/src/components/ClearCompanyResearchModal.tsx:106`. | "Clear" is too weak for deleting generated analysis and emails across many applications. | Use explicit labels: "Archive related applications" and "Delete prep artifacts and reset applications"; require typed confirmation when `n > 0`. |
| [ ] | UXPERF-043 | Medium | Profiles | Profile list lacks search/filter despite profiles being a core entity. | Profiles page only fetches paged summaries and has no search input in `app/src/pages/ProfilesPage.tsx:27`; table begins at `app/src/pages/ProfilesPage.tsx:78`. | With many candidate profiles, users must page manually and cannot find by name/location/email. | Add server-backed search and frozen/active filter. |
| [ ] | UXPERF-044 | Medium | Profiles | Profile edit/create form is one long form with immediate navigation after save. | Form includes identity, education, bio, niche, resume in one card in `app/src/pages/ProfileDetailPage.tsx:226`; save navigates back in `app/src/pages/ProfileDetailPage.tsx:145`. | Long markdown/profile edits lack autosave, dirty warning, and "saved" confirmation in context. | Split into tabs/sections, stay on page after save, show saved timestamp, and add dirty navigation guard. |
| [x] | UXPERF-045 | Low | Profile education | Education row year inputs accept partial/non-year numbers and have no inline validation. | Fixed by full-string 4-digit year validation, 1900-2100 bounds, inline row errors, and save blocking while invalid. | "2024abc" becomes 2024 and invalid years can slip into profile data. | Done. |
| [x] | UXPERF-046 | Medium | Industries | Industry management is split between capped table and full-catalog company picker. | Fixed by standardizing industry access on bounded APIs: paged list plus count for management, bounded options plus selected hydration for company detail, shared frontend API types, and tests for both flows. | Taxonomy UX behaves differently depending on screen: one page cannot browse all, another silently loads all. | Done. Destructive industry delete UX remains separately tracked by UXPERF-020. |
| [x] | UXPERF-047 | Medium | Notifications | Notification messages truncate with no expand/read detail view. | Fixed by inline message expansion controls on every notification row. | Mobile/touch users cannot access full text reliably; important agent messages can be hidden. | Done for expandable row messages. |
| [x] | UXPERF-048 | Medium | Notifications | Delete selected is immediate and optimistic with no confirmation or undo. | Fixed by an in-app confirmation modal before bulk delete executes. | Accidental bulk delete loses notifications with no recovery path. | Done for confirmation-before-delete. Undo remains a future enhancement, not required for this row. |
| [x] | UXPERF-049 | High | Settings | "Release workers" buttons perform high-impact state changes without confirmation. | Fixed by disabling zero-active release buttons and adding an in-app confirmation modal with worker type and active lease count. | Admin can accidentally clear active agent leases, interrupting background work. | Done. |
| [x] | UXPERF-050 | High | Settings/API keys | API key management still has no visible key list or revoke flow. | Fixed by safe public key list endpoint, revoke endpoint, Settings key inventory, last-used/scopes display, and revoke confirmation; raw key/hash are not exposed in list responses. | Admins cannot audit or revoke existing agent credentials from the UI. | Done. |
| [ ] | UXPERF-051 | Medium | Audit | Audit trail lacks entity links, action/entity filters, date filters, and metadata expansion. | UI only filters Actor and displays truncated IDs in `app/src/pages/AuditPage.tsx:52`; table cells show `entity_type / id` text only in `app/src/pages/AuditPage.tsx:86`. | Admin audit screen is hard to investigate from; users must manually copy IDs and cannot inspect metadata. | Add filters for action/entity/date, entity links, metadata drawer, and copy full IDs. |
| [ ] | UXPERF-052 | Medium | Dashboard | Dashboard cards are not actionable and search has no loading or quick filters. | Metrics cards render plain numbers in `app/src/pages/DashboardPage.tsx:50`; search submit only sets results in `app/src/pages/DashboardPage.tsx:23`. | Dashboard shows state but does not help users move into the related work queues. | Link cards to filtered application/notification views, show search busy state, and include recent ready/action-needed rows. |
| [x] | UXPERF-053 | Low | Layout/theme | Theme toggle is not persisted and uses text instead of a familiar icon/toggle state. | Fixed by persisted `jobcrm-theme` state, current-state label, and `aria-pressed` coverage. | User preference resets on reload and the current theme state is unclear. | Done. |
| [ ] | UXPERF-054 | Medium | Error handling | Many errors render at the page top, away from the action that failed. | Application detail has a single top alert at `app/src/pages/ApplicationDetailPage.tsx:245`; Company detail same pattern at `app/src/pages/CompanyDetailPage.tsx:193`. | If a lower-page action fails, user may not see the alert without scrolling back up. | Use contextual inline errors/toasts near the triggering control and keep global alerts for page-load failures. |
| [x] | UXPERF-055 | Medium | Routing | No catch-all route or 404 screen exists. | Fixed by authenticated catch-all 404 route and App route tests. | Mistyped/deleted links render a blank unmatched route, which feels broken. | Done. |

## Priority Order
1. Fix repository persistence and query architecture first: UXPERF-001 through UXPERF-004. These are core scalability blockers.
2. Fix dashboard/search/agent queue hot paths next: UXPERF-006 through UXPERF-009.
3. Fix high-risk frontend UX flows: UXPERF-023, UXPERF-025, UXPERF-028, UXPERF-029, UXPERF-032, UXPERF-033, UXPERF-040, UXPERF-042, UXPERF-049, UXPERF-050.
4. Move page-local filters and full-catalog frontend fetches to bounded server APIs: UXPERF-010 through UXPERF-015 and UXPERF-018.
5. Polish user trust/accessibility flows: UXPERF-016 through UXPERF-022 and UXPERF-024 through UXPERF-055.

## Notes
- Code-review-graph was consulted before file exploration. Graph stats: 91 files, 883 nodes, 9,804 edges. Detailed gap/bridge tools errored in this session, so targeted source reads were used after the available graph scan.
- This doc intentionally records issues only. It does not implement fixes.
