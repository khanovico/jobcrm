# Shared Rules

Canonical assistant-agnostic rule bodies live here.

- Put reusable rule content in this tree.
- Keep assistant-specific discovery wrappers in their own config locations, such as `.cursor/rules/`.
- Wrappers should contain only assistant metadata plus a pointer back to the canonical file here.
