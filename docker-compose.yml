version: "3.9"

services:
  mongo:
    image: mongo:7
    ports:
      - "27017:27017"
    volumes:
      - mongo_data:/data/db

  crm:
    build:
      context: ./crm
    ports:
      - "8511:8500"
    env_file:
      - ./crm/.env
    depends_on:
      - mongo

  app:
    build:
      context: ./app
    ports:
      - "5173:5173"
    env_file:
      - ./app/.env
    depends_on:
      - crm

volumes:
  mongo_data:
